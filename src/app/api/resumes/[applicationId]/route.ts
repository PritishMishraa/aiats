import { get } from "@vercel/blob"; import { eq } from "drizzle-orm"; import { getDb } from "@/db"; import { applications } from "@/db/schema";
export async function GET(_: Request, { params }: { params: Promise<{ applicationId: string }> }) { try { const { applicationId } = await params; const [application] = await getDb().select().from(applications).where(eq(applications.id, applicationId)).limit(1); if (!application) return new Response("Not found", { status: 404 }); const blob = await get(application.resumeUrl, { access: "private" }); if (!blob || blob.statusCode !== 200) return new Response("Not found", { status: 404 }); return new Response(blob.stream, { headers: { "Content-Type": "application/pdf", "Content-Disposition": `inline; filename="${application.resumeName.replace(/[^a-zA-Z0-9._-]/g, "_")}"`, "Cache-Control": "private, no-store" } }); } catch { return new Response("Not found", { status: 404 }); } }
