"use client";
import { useState } from "react"; import Link from "next/link"; import { useRouter } from "next/navigation"; import { ArrowLeft, Check, Info, MagicWand, Sparkle } from "@phosphor-icons/react"; import { Button } from "@/components/ui/button"; import { Textarea } from "@/components/ui/textarea"; import { AdminAccess } from "@/components/admin-access";
const suggestions = ["Team and reporting line", "Experience and core skills", "Location and working model", "Compensation range"];
export default function NewJobPage() { const router = useRouter(); const [prompt, setPrompt] = useState("We need a senior backend engineer with 3+ years of Go, PostgreSQL and distributed systems experience. Bengaluru hybrid, working with the platform team."); const [loading, setLoading] = useState(false); const [error, setError] = useState(""); const [needsAccess, setNeedsAccess] = useState(false); async function generate() { setLoading(true); setError(""); const response = await fetch("/api/jobs/generate", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ prompt }) }); const data = await response.json(); setLoading(false); if (response.status === 401) return setNeedsAccess(true); if (!response.ok) return setError(data.error ?? "Generation failed"); router.push(`/admin/jobs/${data.job.id}`); } return <div className="mx-auto max-w-5xl space-y-7"><div><Link href="/admin/jobs" className="mb-5 inline-flex min-h-10 items-center gap-2 text-xs font-medium text-slate-500 hover:text-slate-950"><ArrowLeft /> Back to jobs</Link><div className="flex items-center gap-3"><span className="grid size-10 place-items-center rounded-xl bg-violet-100 text-violet-700"><MagicWand size={19} weight="fill" /></span><div><h1 className="text-2xl font-semibold tracking-[-0.03em]">Create a new job</h1><p className="mt-1 text-sm text-slate-500">Describe the role naturally. Hireflow will draft a structured job description.</p></div></div></div><div className="grid gap-5 lg:grid-cols-[minmax(0,1.45fr)_minmax(260px,.55fr)]"><section className="rounded-2xl bg-white p-6 shadow-[0_1px_2px_rgba(15,23,42,.05),0_0_0_1px_rgba(15,23,42,.06)]">{needsAccess ? <AdminAccess onSuccess={() => setNeedsAccess(false)} /> : <><label htmlFor="job-prompt" className="text-sm font-semibold">What are you hiring for?</label><p className="mt-1 text-xs text-slate-400">Include the details you know. Assumptions and missing information will be called out.</p><Textarea id="job-prompt" value={prompt} onChange={(event) => setPrompt(event.target.value)} className="mt-5 min-h-52 resize-none rounded-xl border-0 bg-slate-50 p-4 text-sm leading-6 shadow-inner" /><div className="mt-4 flex items-center justify-between gap-3"><p className="text-[11px] text-slate-400">{prompt.length} characters</p><Button onClick={generate} disabled={loading || prompt.trim().length < 20} className="h-10 rounded-lg bg-slate-950 px-4 text-white"><Sparkle weight="fill" className="text-lime-300" /> {loading ? "Generating structured draft…" : "Generate draft"}</Button></div>{error ? <p className="mt-4 rounded-lg bg-red-50 p-3 text-xs text-red-700">{error}</p> : null}</>}</section><aside className="space-y-4"><div className="rounded-xl bg-slate-950 p-5 text-white"><div className="flex items-center gap-2 text-xs font-semibold"><Sparkle className="text-lime-300" weight="fill" /> Better prompts make better drafts</div><ul className="mt-4 space-y-3">{suggestions.map((item) => <li key={item} className="flex items-center gap-2 text-[11px] text-slate-300"><Check className="text-lime-300" />{item}</li>)}</ul></div><div className="flex gap-3 rounded-xl bg-amber-50 p-4 text-amber-900"><Info size={17} className="mt-0.5 shrink-0" /><p className="text-[11px] leading-5">AI output is always a draft. You’ll approve the job and its scoring rubric before candidates are evaluated.</p></div></aside></div></div>; }
