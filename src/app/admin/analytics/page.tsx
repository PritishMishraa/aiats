import { sql } from "drizzle-orm"; import { getDb } from "@/db"; import { applications, jobs } from "@/db/schema";
export const dynamic = "force-dynamic";
export default async function AnalyticsPage() { const [counts] = await getDb().select({ jobs: sql<number>`count(distinct ${jobs.id})`, applications: sql<number>`count(distinct ${applications.id})`, avgScore: sql<number>`coalesce(avg(${applications.weightedScore}), 0)`, strongFits: sql<number>`count(*) filter (where ${applications.recommendation} = 'strong_fit')` }).from(jobs).leftJoin(applications, sql`${applications.jobId} = ${jobs.id}`); const metrics = [["Jobs", counts.jobs], ["Applications", counts.applications], ["Strong fits", counts.strongFits], ["Average score", Math.round(counts.avgScore)]]; return <div className="space-y-6"><div><p className="text-xs font-medium text-slate-400">Reporting</p><h1 className="mt-1 text-2xl font-semibold">Analytics</h1><p className="mt-1 text-sm text-slate-500">Live aggregate metrics from your hiring pipeline.</p></div><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{metrics.map(([label, value]) => <div key={label} className="rounded-xl bg-white p-5 shadow-[0_0_0_1px_rgba(15,23,42,.05)]"><p className="text-xs text-slate-500">{label}</p><p className="mt-3 tabular-nums text-3xl font-semibold">{value}</p></div>)}</div></div>; }
