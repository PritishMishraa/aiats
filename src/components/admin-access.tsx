"use client";
import { useState } from "react"; import { Button } from "@/components/ui/button"; import { Input } from "@/components/ui/input";
export function AdminAccess({ onSuccess }: { onSuccess?: () => void }) { const [email, setEmail] = useState(""); const [error, setError] = useState(""); const [loading, setLoading] = useState(false); async function submit() { setLoading(true); setError(""); const response = await fetch("/api/admin/session", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ email }) }); const data = await response.json(); setLoading(false); if (!response.ok) return setError(data.error); onSuccess?.(); location.reload(); } return <div className="rounded-xl bg-amber-50 p-4"><p className="text-xs font-semibold text-amber-950">Confirm your admin email</p><p className="mt-1 text-[11px] text-amber-700">Protected operations require one of the two demo allowlisted emails.</p><div className="mt-3 flex gap-2"><Input type="email" placeholder="you@company.com" value={email} onChange={(event) => setEmail(event.target.value)} className="h-9 bg-white"/><Button onClick={submit} disabled={loading} className="h-9 bg-slate-950 text-white">{loading ? "Checking…" : "Continue"}</Button></div>{error ? <p className="mt-2 text-[11px] text-red-600">{error}</p> : null}</div>; }
