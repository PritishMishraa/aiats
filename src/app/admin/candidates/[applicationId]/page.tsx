import Link from "next/link"; import { eq } from "drizzle-orm"; import { notFound } from "next/navigation"; import { ArrowLeft, CheckCircle, FilePdf, Warning } from "@phosphor-icons/react/dist/ssr"; import { Badge } from "@/components/ui/badge"; import { CandidateActions } from "@/components/candidate-actions"; import { getDb } from "@/db"; import { applications, interviews, jobs } from "@/db/schema";
export const dynamic = "force-dynamic";
export default async function CandidatePage({ params }: PageProps<"/admin/candidates/[applicationId]">) { const { applicationId } = await params; const [row] = await getDb().select({ application: applications, job: jobs, interview: interviews }).from(applications).innerJoin(jobs, eq(applications.jobId, jobs.id)).leftJoin(interviews, eq(interviews.applicationId, applications.id)).where(eq(applications.id, applicationId)).limit(1); if (!row) notFound(); const { application, job, interview } = row; const weights = new Map(job.rubric?.criteria.map((item) => [item.id, item]) ?? []); return <div className="space-y-6"><Link href="/admin/candidates" className="inline-flex min-h-10 items-center gap-2 text-xs font-medium text-slate-500"><ArrowLeft/> Candidate queue</Link><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><div className="flex items-center gap-2"><Badge variant="outline">{application.status.replaceAll("_", " ")}</Badge>{application.needsHumanReview ? <span className="flex items-center gap-1 text-[11px] text-amber-700"><Warning weight="fill"/> Human review required</span> : null}</div><h1 className="mt-3 text-2xl font-semibold">{application.candidateName}</h1><p className="mt-1 text-sm text-slate-500">{job.title} · {application.candidateEmail}</p></div><div className="flex gap-2"><a href={`/api/resumes/${application.id}`} target="_blank" className="inline-flex h-10 items-center gap-2 rounded-lg border border-slate-200 px-3 text-xs font-medium"><FilePdf/> Resume</a><CandidateActions applicationId={application.id} invited={Boolean(interview)} /></div></div><div className="grid gap-5 lg:grid-cols-[1.35fr_.65fr]"><section className="rounded-xl bg-white p-5 shadow-[0_0_0_1px_rgba(15,23,42,.05)]"><div className="flex items-end justify-between"><div><h2 className="text-sm font-semibold">AI recommendation</h2><p className="mt-1 text-xs capitalize text-slate-500">{application.recommendation?.replaceAll("_", " ") ?? "Evaluation pending"}</p></div><p className="tabular-nums text-3xl font-semibold">{application.weightedScore ?? "—"}<span className="text-xs font-normal text-slate-400"> / 100</span></p></div><div className="mt-6 space-y-3">{application.evaluation?.criteria.map((result) => { const criterion = weights.get(result.criterionId); return <div key={result.criterionId} className="rounded-xl bg-slate-50 p-4"><div className="flex justify-between"><div><p className="text-xs font-semibold">{criterion?.name ?? result.criterionId}</p><p className="mt-1 text-[10px] capitalize text-slate-400">{result.confidence} confidence · {criterion?.weight ?? 0}% weight</p></div><p className="tabular-nums text-sm font-semibold">{result.score} / 5</p></div><p className="mt-3 text-[11px] leading-5 text-slate-600">{result.reasoning}</p>{result.evidence.length ? <div className="mt-3 space-y-1">{result.evidence.map((evidence) => <p key={evidence} className="text-[10px] italic text-slate-500">“{evidence}”</p>)}</div> : <p className="mt-2 text-[10px] text-amber-700">No evidence found in resume</p>}</div>; })}</div></section><aside className="space-y-4"><div className="rounded-xl bg-white p-5 shadow-[0_0_0_1px_rgba(15,23,42,.05)]"><h2 className="text-sm font-semibold">Assessment summary</h2><p className="mt-3 text-xs leading-6 text-slate-600">{application.evaluation?.summary ?? application.failureReason ?? "Processing is still underway."}</p></div>{application.evaluation?.missingEvidence.length ? <div className="rounded-xl bg-amber-50 p-5"><h2 className="text-xs font-semibold text-amber-900">Missing evidence</h2><ul className="mt-2 list-disc space-y-1 pl-4 text-[11px] text-amber-800">{application.evaluation.missingEvidence.map((item) => <li key={item}>{item}</li>)}</ul></div> : null}{interview?.bookingUrl ? <a href={interview.bookingUrl} target="_blank" className="flex items-center gap-2 rounded-xl bg-emerald-50 p-4 text-xs font-semibold text-emerald-800"><CheckCircle weight="fill"/> Open candidate booking link</a> : null}</aside></div></div>; }
